# GitHub Pages 上傳

此版本只把原本內嵌在 HTML 的大型圖片拆到 `assets/`，遊戲畫面、計分與互動邏輯不變。

1. 將 `index.html` 與整個 `assets` 資料夾一起上傳到 repository 根目錄。
2. GitHub → Settings → Pages。
3. Source 選 `Deploy from a branch`。
4. Branch 選 `main`、資料夾選 `/(root)`，儲存。

請勿只上傳 index.html。
